#include"uart.h"
// #include"printf.h" 
#include"page.h" 

int main(void) {
	// mem_init();
	print("Entered Main\n");
	// float f = 0.5f; 
	// float f2 = 0.75f;
	// float sum = f + f2; 
	// printf("%f + %f = %f\n", f, f2, sum);
}